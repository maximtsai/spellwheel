 class Dummy extends Enemy {
    constructor(scene, x, y) {
        super(scene, x, y);
        this.initSprite('gobbo0.png', 0.56);
    }

    // update(dt) {}

    // reset(x, y) {
    //     this.x = x;
    //     this.y = y;
    // }

}
